{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const fs=require('fs');if(fs.existsSync('docs/software-architect/fa-context.json'))process.stdout.write(JSON.stringify({additionalContext:'A previous architect run was interrupted: docs/software-architect/fa-context.json still exists (it is normally deleted at the end of /software-architect:deliver). Ask the user whether to resume from this context or start fresh.'}));\""
          }
        ]
      }
    ]
  }
}
