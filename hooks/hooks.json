{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f docs/architect/fa-context.json ]; then echo '{\"additionalContext\": \"A previous project analysis (fa-context.json) was found in docs/architect/. Ask the user if they want to continue from it or start fresh.\"}'; fi"
          }
        ]
      }
    ]
  }
}
